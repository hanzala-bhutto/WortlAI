# WortlAI backend - Python 3.12
#
# Each issue adds its own dependencies alongside the code that needs them, so a
# fresh clone always installs a set that is actually exercised: edge-tts +
# langgraph (#3, #4), sqlalchemy + alembic (#6), py-fsrs + llamaindex +
# qdrant-client (Phase 2). langfuse below is #7 (tracing + prompt management).

fastapi==0.140.0
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2
# HTTP client. httpx2 is the next-gen line (pydantic/httpx2); Starlette 1.3's
# TestClient uses it and deprecates plain httpx. We depend on httpx2 directly so
# both our own async client and the test client run on it - see app/llm/provider.py.
httpx2==2.9.1

# LLMOps: tracing + prompt management (#7). Pulls in the OpenTelemetry SDK.
langfuse==4.14.1

# Session orchestration: the conversation as a checkpointed state graph (#4). The
# checkpointer gets its own SQLite file (checkpoints.db) via aiosqlite, separate
# from wortlai.db - two writers, no shared connection.
langgraph==1.2.9
langgraph-checkpoint-sqlite==3.1.0
# The async SQLite driver the checkpointer's connection is opened with. A
# transitive dep of the checkpointer, pinned here because we import it directly.
aiosqlite==0.22.1

# Voice out: German neural TTS (#3), behind app/voice/tts.py. Unofficial endpoint
# (Qwen3-TTS is the documented fallback), so it stays quarantined behind that one
# interface. STT is Groq Whisper over httpx2, so it needs no extra dependency.
edge-tts==7.2.8

# Learner persistence: sessions, error log, immersion hours (#6). Alembic from
# day one because the schema grows every phase (FSRS + graph tables in Phase 2).
sqlalchemy==2.0.36
alembic==1.14.0

# RAG ingestion: deterministic glossary parsing (#9). Chosen over PyMuPDF for
# char-level position access (page.chars, extract_words x_tolerance) needed to
# work around the source PDF's font-level dropped-vowel bug - see
# app/rag/glossary_parser.py's module docstring.
pdfplumber==0.11.10

# RAG embedding + retrieval (#11): embedder.py behind one swappable interface
# (docs/feasibility/011-embedder-qdrant-collections.md), two Qdrant collections
# loaded via LlamaIndex. sentence-transformers pulls in torch for local e5-large
# CPU inference; llama-index-embeddings-huggingface is only used for its
# query_instruction/text_instruction hook onto that model, not for NIM (a direct
# httpx2 call instead, since the NVIDIA embeddings package doesn't expose the
# `truncate` field nv-embedqa-e5-v5 requires).
llama-index-core==0.14.23
llama-index-vector-stores-qdrant==0.10.2
llama-index-embeddings-huggingface==0.7.0
qdrant-client==1.18.0
sentence-transformers==5.6.1

# Dev / test
pytest==9.1.1
pytest-asyncio==1.4.0
# Lint + format (#47). ruff owns both; pre-commit runs it on every commit. Config
# lives in pyproject.toml [tool.ruff]; see .pre-commit-config.yaml at the repo root.
ruff==0.16.0
pre-commit==4.6.1
